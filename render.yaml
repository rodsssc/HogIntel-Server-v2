services:
  - type: web
    name: hogintel-api
    env: docker
    plan: free
    dockerfilePath: docker/Dockerfile   # 👈 tell Render where to find it
    envVars:
      - key: ENVIRONMENT
        value: production
      - key: HOST
        value: 0.0.0.0
      - key: PORT
        value: 8000
